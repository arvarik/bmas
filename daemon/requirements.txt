fastapi>=0.137.0        # Requires lifespan= parameter (added in 0.93)
uvicorn[standard]>=0.49.0  # ASGI server with uvloop + httptools
httpx>=0.28.1           # Async HTTP client for agent dispatch
redis[hiredis]>=8.0.0   # Async Redis with C-accelerated parser (RESP3 default, legacy shapes preserved)
pydantic>=2.13.4        # Request/response validation
pyyaml>=6.0.3           # bmas.yaml config file loading
aiosqlite>=0.22.0       # Async SQLite for task history persistence
pymupdf>=1.27.0         # PDF text extraction (doc 17 §3)
python-multipart>=0.0.32  # FastAPI multipart form data (file uploads, doc 17 §3)
