"""The bMAS agent protocol: vendored digest profile and signing, plus the native activation flow.

The digest profile and the signing module are byte-for-byte copies of
the daemon modules. A repository test keeps them equal.
"""
