# bMAS Cleaner Profile, reviewed for Hermes Agent v0.20.4.
# The cleaner receives board content in its prompt and has no tools.

model:
  default: "medium"
  provider: "custom"
  base_url: "http://192.168.4.240:4000/v1"
  api_key: "${LITELLM_MASTER_KEY}"

toolsets: []

platform_toolsets:
  cli: []
  api_server: []
