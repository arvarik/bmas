# bMAS Conflict Resolver Profile, reviewed for Hermes Agent v0.20.4.
# The file toolset also includes write and patch tools.

model:
  default: "medium"
  provider: "custom"
  base_url: "http://192.168.4.240:4000/v1"
  api_key: "${LITELLM_MASTER_KEY}"

toolsets:
  - web
  - browser
  - file
  - memory

platform_toolsets:
  cli:
    - web
    - browser
    - file
    - memory
  api_server:
    - web
    - browser
    - file
    - memory
