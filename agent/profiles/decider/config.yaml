# bMAS Decider Profile, reviewed for Hermes Agent v0.20.4.
# The decider receives board content in its prompt.

model:
  default: "medium"
  provider: "custom"
  base_url: "http://192.168.4.240:4000/v1"
  api_key: "${LITELLM_MASTER_KEY}"

toolsets:
  - web
  - memory

platform_toolsets:
  cli:
    - web
    - memory
  api_server:
    - web
    - memory
